interface:
  display_name: "Element Maze Session Ops"
  short_description: "Handoffs and GUI prototype checks"
  default_prompt: "Use $element-maze-session-ops to prepare a handoff, decide session continuation, or give GUI prototype server URLs."
